{
  "name": "whatfees-realtime",
  "version": "1.0.149",
  "private": true,
  "description": "whatfees realtime websocket gateway",
  "main": "dist/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p .",
    "start": "node --env-file=.env.local dist/index.js",
    "start:dev": "npm run build && node --env-file=.env.local dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.mjs",
    "verify": "npm run build && npm run test"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.18.6",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.3"
  }
}
